% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orm_mle0.R
\name{orm_mle0}
\alias{orm_mle0}
\title{MLE0 Estimator under two-phase sampling designs for ordinal outcomes}
\usage{
orm_mle0(
  formula,
  data,
  x_name,
  family = "probit",
  theta_init = NULL,
  se_calc = TRUE,
  se_method = "forward",
  verbose = TRUE,
  max_iter = 500,
  tol = 1e-06,
  se_max_iter = 1000,
  se_tol = 1e-08
)
}
\arguments{
\item{formula}{Formula for the ordinal outcome Y.}

\item{data}{Data frame. NA allowed only in \code{x_name} for S=0 subjects.}

\item{x_name}{Name of the expensive covariate X (used to identify Phase 2 subjects via missingness).}

\item{family}{Character value specifying the distribution family, which is one of the following: "logit", "probit"}

\item{theta_init}{Optional initial vector for theta (beta, cutpoints).}

\item{se_calc}{Logical; whether to compute standard errors. Defaults to TRUE.}

\item{se_method}{Character value specifying the method for SE estimation: "forward" uses second-order forward-difference Hessian (default) and "numDeriv" uses Richardson extrapolation via \code{numDeriv::hessian()}.}

\item{verbose}{Logical; if TRUE, prints convergence info after estimation. Defaults to FALSE.}

\item{max_iter}{Maximum number of EM iterations. Defaults to 500.}

\item{tol}{Convergence tolerance for the optimizer. Defaults to 1e-6.}

\item{se_max_iter}{Maximum number of EM iterations for finding optimal q_j in SE estimation. Defaults to 1000.}

\item{se_tol}{Convergence tolerance for the optimizer in SE estimation. Defaults to 1e-8.}
}
\value{
A list containing:
\item{est}{Estimated parameter vector (beta, cutpoints).}
\item{se}{Standard errors (if \code{se_calc = TRUE}).}
\item{vcov}{Variance-covariance matrix (if \code{se_calc = TRUE}).}
\item{q_j}{Estimated discrete distribution over support points.}
\item{iterations}{Number of EM iterations.}
\item{se_max_iterations}{Maximum inner EM iterations during SE estimation.}
}
\description{
A special case of SMLE that treats all covariates as missing for Phase 1-only subjects
and models the covariate distribution as a discrete distribution over the unique
covariate combinations observed in Phase 2. Does not require a B-spline basis.
}
